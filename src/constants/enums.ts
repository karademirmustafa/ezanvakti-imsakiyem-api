export enum LocationTypes {
    COUNTRIES = "countries",
    STATES = "states",
    DISTRICTS = "districts",
  }
  

  export enum PrayerTimePeriods {
    DAILY = "daily",
    WEEKLY = "weekly",
    MONTHLY = "monthly",
    YEARLY = "yearly",
    RANGE = "range"
  }
  